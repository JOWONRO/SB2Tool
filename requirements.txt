# Beta4.0 배포 빌드 기준 = Python 3.8 (python38.dll, *.cp38-win_amd64.pyd)
# 4.1도 동일하게 3.8로 빌드한다. 아래 버전은 전부 3.8 지원 확인된 것.

PyQt5==5.15.10
psutil==5.9.8
pywin32==306
keyboard==0.13.5
pyautogui==0.9.54
clipboard==0.0.4

# 0.22.5 이상 필요.
# - 하드코딩 버전 표에 2024 매핑이 들어간 건 0.21.9(2023-08-31),
#   2025 매핑은 0.22.5(2024-11-01). Beta4.0이 번들한 0.21.x 이하에는 둘 다 없어서
#   CC2024에서 멈춤/강제종료가 발생했다.
# - 0.22.2부터 COM Dispatch 초기화가 레지스트리 조회 기반으로 바뀌어, 버전 표에
#   없는 포토샵도 인식하고 실행 중인 인스턴스에 먼저 붙는다.
# - 0.22.10은 python >=3.8,<4.0 지원 확인됨 (0.23 이상은 미확인이라 상한을 둠).
# - 주의: 0.22.x부터 내부 COM 백엔드가 pywin32 -> comtypes로 교체됐다.
#   comtypes가 새로 딸려 들어오고, ps_app 객체 동작이 미묘하게 다를 수 있으므로
#   업그레이드 후 포토샵 모드 실기 테스트 필수.
photoshop_python_api>=0.22.5,<0.23
